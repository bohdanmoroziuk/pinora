info:
  name: Get pin by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/pins/{{PIN_ID}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
