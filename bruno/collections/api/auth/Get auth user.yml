info:
  name: Get auth user
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/auth/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
