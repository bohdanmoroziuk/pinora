info:
  name: Create comment
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/pins/{{PIN_ID}}/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
