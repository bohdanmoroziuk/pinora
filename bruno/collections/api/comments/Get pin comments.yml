info:
  name: Get pin comments
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/pins/{{PIN_ID}}/comments"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
