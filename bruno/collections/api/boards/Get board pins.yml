info:
  name: Get board pins
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/pins?boardId={{BOARD_ID}}"
  params:
    - name: boardId
      value: "{{BOARD_ID}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
