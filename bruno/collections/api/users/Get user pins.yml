info:
  name: Get user pins
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/pins?userId={{USER_ID}}"
  params:
    - name: userId
      value: "{{USER_ID}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
