info:
  name: Get user boards
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/boards?userId={{USER_ID}}"
  params:
    - name: userId
      value: "{{USER_ID}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
