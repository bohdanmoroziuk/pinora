info:
  name: Get user
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/users/{{USERNAME}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
